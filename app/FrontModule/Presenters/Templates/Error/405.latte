{var $robots = noindex}

{block content}
	{default $children = '.failGracefully(TRUE);'}
	{include ../@pageHeader.latte, children => $children}

	<div class="wrapper">
		<h2 n:block=title>Method Not Allowed</h2>

		<p>The requested method is not allowed for the URL.</p>

		<p>
			<small>error 405</small>
		</p>
	</div>
{/block}
